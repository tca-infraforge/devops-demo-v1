apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-clock.fullname" . }}
  labels:
    {{- include "devops-clock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-clock.selectorLabels" . | nindent 4 }}
