apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-clock.fullname" . }}-test-connection"
  labels:
    {{- include "devops-clock.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-clock.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
